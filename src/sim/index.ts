/**
 * sim/ is the headless balance harness.
 *
 * Imports engine + content, never app. Plays thousands of seeded runs with a
 * heuristic policy and reports win rate per node, deaths per enemy, card pick and
 * win rates, run length, the HP curve by depth, and Interest pressure over time.
 *
 * This is where balance decisions come from. Phase 2 builds it.
 */
export {};
